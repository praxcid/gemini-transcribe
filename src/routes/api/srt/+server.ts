// Placeholder file: SRT endpoint slated for full removal.
// If this file still appears after deletion attempts, remove the folder `src/routes/api/srt` manually.
export const POST = () => new Response('Gone', { status: 410 });
// (Removed stray PowerShell command that broke build)
